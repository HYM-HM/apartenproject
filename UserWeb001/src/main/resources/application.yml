spring:
  application:
    name: UserWeb01
  thymeleaf:
    cache: false
server:
  port: 7001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka,http://localhost:9999/eureka
